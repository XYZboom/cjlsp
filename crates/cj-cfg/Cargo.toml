[package]
name = "cj-cfg"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Lightweight CFG + dataflow analysis over Cangjie AST (maybe-uninit, const propagation)"

[dependencies]
cj-ast = { path = "../cj-ast" }